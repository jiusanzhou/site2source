# SiteModel 集成到 Site2Source 插件 — 分阶段方案

## 目标
让插件用户不再需要写代码/命令行, 通过 UI 生成"aiyifan 这类 SPA + 签名站"的 spider。

## 分三阶段, 每阶段独立可用

### 阶段 1: SiteModel 手工编辑器 (今天)
**入口**: workbench 加一个"SiteModel"tab (或菜单项)
**功能**:
1. 加载内置模板 (aiyifan)
2. 表单编辑 signing.modes / endpoints / mappings
3. **实时预览生成的 spider.js**
4. **一键复制/下载 spider + tvbox.json**
5. **签名验证器 UI**: 粘贴一批抓包 URL, 立即校验通过率 + 诊断

**收益**: 已有 SiteModel 的人 (比如以后我逆完别的站) 30 秒出 spider。

### 阶段 2: 抓包辅助 (2-3 天)
利用插件已有的 XHR 抓包能力, 半自动填 SiteModel:
1. 页面上抓 XHR → 识别可能的 API 端点 (`/api/`, `.json`, JSON 响应等)
2. 自动检测签名参数 (32 hex = md5, 短参数 = timestamp)
3. 让用户点选"这是 home"、"这是 detail" (类似现有 role picker)
4. 从抓包填 endpoints 数组

**收益**: 简单站 (无签名或签名从 header) 全自动出草稿, 用户点几下确认。

### 阶段 3: 签名逆向助手 (1 周)
1. 从当前站的 main.js bundle 抓字符串 → 找可能的密钥表
2. 找 `md5(...)` / `hmac(...)` 调用点
3. 生成 SignMode 候选, **用验证器校验**通过率
4. 通过率 100% = 找到; 部分通过 = 显示诊断给用户

**收益**: aiyifan 这种签名站, 20 分钟出草稿而不是 3 小时。

---

## 阶段 1 具体分解 (今天目标)

### 文件改动
- 新增 `sitemodel/editor.tsx` — 主编辑器组件
- 新增 `sitemodel/verifier.tsx` — 签名验证器 UI
- 新增 `sitemodel/preview.tsx` — spider 预览 + 下载
- 修改 `workbench.tsx` — 加入口按钮
- 修改 `lib/site-model.ts` — 加 JSON 序列化/反序列化 helper

### UI 草图
```
┌─────────────────────────────────────────┐
│ [Roles] [SiteModel] [Settings]          │ ← tab
├─────────────────────────────────────────┤
│ 模板: [aiyifan ▼] [新建]  [导入 JSON]   │
├─────────────────────────────────────────┤
│ 基本信息                                │
│   name: aiyifan                         │
│   site_url: https://...                 │
├─────────────────────────────────────────┤
│ 签名 [+] 添加模式                       │
│   ┌─ timestamp ─────────────────────┐  │
│   │ formula: {pub}&{q}&{pk}         │  │
│   │ algorithm: md5                  │  │
│   │ pk 密钥表: [8 个]  [+]          │  │
│   │ [🔍 验证 (粘贴 10 组样本)]      │  │
│   └─────────────────────────────────┘  │
├─────────────────────────────────────────┤
│ 端点 [+] 添加                           │
│   home / detail / play / search ...     │
├─────────────────────────────────────────┤
│ 字段映射                                │
├─────────────────────────────────────────┤
│ [👀 预览 spider.js] [💾 下载] [📋 复制] │
└─────────────────────────────────────────┘
```
